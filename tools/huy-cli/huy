#!/usr/bin/env python3
"""Huygens CLI — Phase 9 SSH (`huy ssh`)."""

from __future__ import annotations

import argparse
import json
import os
import select
import sys
import termios
import tty
import urllib.error
import urllib.request


def _token() -> str:
    token = os.environ.get("HUY_ACCESS_TOKEN", "")
    if not token:
        print("Set HUY_ACCESS_TOKEN or run: export HUY_ACCESS_TOKEN=$(huy login ...)", file=sys.stderr)
        sys.exit(1)
    return token


def _api(base: str, path: str, *, method: str = "GET", body: dict | None = None) -> dict:
    data = None
    headers = {"Authorization": f"Bearer {_token()}"}
    if body is not None:
        data = json.dumps(body).encode()
        headers["Content-Type"] = "application/json"
    req = urllib.request.Request(
        f"{base.rstrip('/')}{path}",
        data=data,
        headers=headers,
        method=method,
    )
    try:
        with urllib.request.urlopen(req, timeout=30) as resp:
            return json.loads(resp.read().decode())
    except urllib.error.HTTPError as exc:
        detail = exc.read().decode()
        print(f"API error {exc.code}: {detail}", file=sys.stderr)
        sys.exit(1)


def _gateway_ws_url(gateway: str, session_id: str) -> str:
    base = gateway.rstrip("/")
    if base.startswith("https://"):
        return "wss://" + base[len("https://") :] + f"/api/v1/ssh/sessions/{session_id}/ws"
    if base.startswith("http://"):
        return "ws://" + base[len("http://") :] + f"/api/v1/ssh/sessions/{session_id}/ws"
    return f"ws://{base}/api/v1/ssh/sessions/{session_id}/ws"


def _attach_session_ws(gateway: str, session_id: str) -> None:
    try:
        import websocket  # type: ignore
    except ImportError:
        print(
            "Interactive SSH needs websocket-client: pip install -r tools/huy-cli/requirements.txt",
            file=sys.stderr,
        )
        sys.exit(1)

    url = _gateway_ws_url(gateway, session_id)
    headers = [f"Authorization: Bearer {_token()}"]
    ws = websocket.create_connection(url, header=headers, suppress_origin=True)

    old_tty = termios.tcgetattr(sys.stdin.fileno())
    try:
        tty.setraw(sys.stdin.fileno())
        while True:
            rlist, _, _ = select.select([sys.stdin, ws.sock], [], [])
            if sys.stdin in rlist:
                data = os.read(sys.stdin.fileno(), 4096)
                if not data:
                    break
                ws.send_binary(data)
            if ws.sock in rlist:
                opcode, frame = ws.recv_data()
                if opcode in (websocket.ABNF.OPCODE_CLOSE, websocket.ABNF.OPCODE_NONE):
                    break
                if frame:
                    os.write(sys.stdout.fileno(), frame)
    finally:
        termios.tcsetattr(sys.stdin.fileno(), termios.TCSADRAIN, old_tty)
        ws.close()


def cmd_ssh(args: argparse.Namespace) -> None:
    gateway = os.environ.get("HUY_SSH_GATEWAY_URL", "http://127.0.0.1:8087")
    org = args.org or os.environ.get("HUY_ORG_ID", "")
    if not org:
        print("--org or HUY_ORG_ID required", file=sys.stderr)
        sys.exit(1)
    session = _api(
        gateway,
        "/api/v1/ssh/sessions",
        method="POST",
        body={
            "organization_id": org,
            "project_id": args.project,
            "vm_name": args.vm,
        },
    )
    if args.json or not sys.stdin.isatty() or not sys.stdout.isatty():
        print(json.dumps(session, indent=2))
        print(f"\nWebSocket: {_gateway_ws_url(gateway, session['id'])}")
        return
    print(f"Connecting to {session['vm_name']} as {session['linux_user']}…", file=sys.stderr)
    try:
        _attach_session_ws(gateway, session["id"])
    except KeyboardInterrupt:
        pass


def cmd_ssh_proxy(args: argparse.Namespace) -> None:
    """OpenSSH ProxyCommand helper — opens gateway WebSocket tunnel on stdio."""
    try:
        import websocket  # type: ignore
    except ImportError:
        print("pip install -r tools/huy-cli/requirements.txt", file=sys.stderr)
        sys.exit(1)

    gateway = os.environ.get("HUY_SSH_GATEWAY_URL", "http://127.0.0.1:8087")
    org = os.environ.get("HUY_ORG_ID", "")
    project = os.environ.get("HUY_PROJECT_ID", "")
    vm = args.host.split(".")[0]
    session = _api(
        gateway,
        "/api/v1/ssh/sessions",
        method="POST",
        body={"organization_id": org, "project_id": project, "vm_name": vm},
    )
    url = _gateway_ws_url(gateway, session["id"])
    headers = [f"Authorization: Bearer {_token()}"]
    ws = websocket.create_connection(url, header=headers, suppress_origin=True)
    try:
        while True:
            rlist, _, _ = select.select([sys.stdin, ws.sock], [], [])
            if sys.stdin in rlist:
                data = os.read(sys.stdin.fileno(), 4096)
                if not data:
                    break
                ws.send_binary(data)
            if ws.sock in rlist:
                opcode, frame = ws.recv_data()
                if opcode in (websocket.ABNF.OPCODE_CLOSE, websocket.ABNF.OPCODE_NONE):
                    break
                if frame:
                    os.write(sys.stdout.fileno(), frame)
    finally:
        ws.close()


def main() -> None:
    parser = argparse.ArgumentParser(prog="huy")
    sub = parser.add_subparsers(dest="command", required=True)
    ssh_p = sub.add_parser("ssh", help="Create audited SSH session")
    ssh_p.add_argument("vm", help="VM name")
    ssh_p.add_argument("--project", required=True)
    ssh_p.add_argument("--org", default=None)
    ssh_p.add_argument("--json", action="store_true", help="Print session JSON only; do not connect")
    ssh_p.set_defaults(func=cmd_ssh)
    proxy_p = sub.add_parser("ssh-proxy", help="OpenSSH ProxyCommand helper")
    proxy_p.add_argument("host")
    proxy_p.add_argument("port")
    proxy_p.set_defaults(func=cmd_ssh_proxy)
    args = parser.parse_args()
    args.func(args)


if __name__ == "__main__":
    main()
