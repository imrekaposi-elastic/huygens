[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "huy-libvirt-agent"
version = "0.1.0"
description = "KVM hypervisor agent: VMs, vnets, breakout networking, DNAT"
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "pydantic>=2.9.0",
    "pydantic-settings>=2.6.0",
    "structlog>=24.4.0",
    "httpx>=0.27.0",
    "jinja2>=3.1.0",
    "pyyaml>=6.0.0",
    "huy-telemetry",
    "prometheus-client>=0.21.0",
    "psutil>=6.0.0",
    "cryptography>=43.0.0",
    "jsonschema>=4.0.0",
]

[tool.uv.sources]
huy-telemetry = { path = "../../shared/huy_telemetry", editable = true }

[project.optional-dependencies]
libvirt = ["libvirt-python>=10.0.0"]
dev = [
    "pytest>=8.3.0",
    "pytest-asyncio>=0.24.0",
    "httpx>=0.27.0",
    "ruff>=0.7.0",
]

[project.scripts]
huy-libvirt-agent = "huy_libvirt_agent.main:run"

[tool.hatch.build.targets.wheel]
packages = ["src/huy_libvirt_agent"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["src", "../../shared/huy_telemetry/src"]

[tool.ruff]
line-length = 110
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
